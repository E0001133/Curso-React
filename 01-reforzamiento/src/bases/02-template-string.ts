const firstName = 'Harold';
const lastName = "0'neal";

/*Esto es una forma de concatenar variables string */
const fullName = `El nombre es: ${firstName} ${lastName}`;
console.log(fullName);

console.log(firstName + ' ' + lastName + ':');
